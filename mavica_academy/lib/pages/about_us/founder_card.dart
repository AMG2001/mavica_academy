import 'package:flutter/material.dart';

class FounderCard extends StatelessWidget {
  const FounderCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(children: []),
    );
  }
}
